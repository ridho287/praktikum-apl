#include <iostream>
using namespace std;

int main() {
    // string nama;
    // cout < "Masukkan nama kamu: ";
    // cin > nama;
    // cout < "Halo, " < nama;
    int apel;
    int melon;
    cout << "Apakah jumlah Apel == Melon? : " << (apel == melon) << endl;
    cout << "Apakah jumlah Apel != Melon? : " << (apel != melon) << endl;
    cout << "Apakah jumlah Apel > Melon? : " << (apel > melon) << endl;
    cout << "Apakah jumlah Apel < Melon? : " << (apel < melon) << endl;
    cout << "Apakah jumlah Apel >= Melon? : " << (apel >= melon) << endl;
    cout << "Apakah jumlah Apel <= Melon? : " << (apel <= melon) << endl;

    return 0;
}